# We use the vaulted penumltimate dot release to ensure a frozen dnf repo.
# We use a artifactory remote repository hosted in the Arista network which
# is configured to mirror the upstream almalinux repos.
DNF_DISTRO_REPO_VERSION="9.3"
DNF_DISTRO_REPO_RELEASE="1.el9"
DNF_EPEL_SNAPSHOT_REPO="eext-snapshots-local/epel9"
DNF_EPEL_SNAPSHOT_REPO_VERSION="v20240127-1"
DNF_EPEL_DISTRO_VERSION="9"
ARCH=$(arch)

