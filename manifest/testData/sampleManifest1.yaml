---
package:
  - name: libpcap
    subdir: true
    upstream:
      - http://foo/libpcap.src.rpm
    type: srpm
    build:
      repo:
        - name: BaseOS
          version: foo/bar
  - name: tcpdump
    upstream:
      - http://foo/tcpdump.src.rpm
    type: srpm
    build:
      repo:
        - name: BaseOS
          version: foo/bar
